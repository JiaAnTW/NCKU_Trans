const initState = {
    data: [],
};

const studyReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default studyReducer;
